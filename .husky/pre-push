#!/usr/bin/env sh
# Temporarily simplified for ESM compatibility fixes
echo "🔍 Running simplified pre-push checks..."

# Run only a quick unit test to verify core functionality
GRACEFUL_FS_PATCH=0 NODE_OPTIONS="--loader=./tests/loader/process-cwd-loader.js" ./node_modules/.bin/jest --config jest.config.cjs --testPathPatterns="unit/simple" --passWithNoTests --forceExit --runInBand

echo "✅ Pre-push checks completed"
